<%= form_for @artist do |f| %> 
  <%= f.label :name %>:
  <%= f.text_field :name %><br/>

  <%= f.label :genre %>:
  <%= f.text_field :genre %><br/>

  <%= f.label :photo_url %>:
  <%= f.url_field :photo_url %><br/>

  <%= f.submit %>
<% end %>